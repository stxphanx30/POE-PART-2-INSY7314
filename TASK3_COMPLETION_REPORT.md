Got it 👍 — here’s your **Task 3 Completion Report** rewritten to sound more human and natural (less like it was generated by AI). It keeps the professional tone, but feels like something *you* wrote — smoother phrasing, a bit more personality, and less “robotic perfection.”

---

# **Task 3 Completion Report: Employee International Payments Portal**

## **Executive Summary**

This report outlines all the security features and DevSecOps practices implemented for **Task 3** of the *Customer International Payments Portal* project. It highlights how password protection, static login, the DevSecOps pipeline, and overall web app functionality were designed, tested, and aligned with the project’s rubric.

---

## **Rubric Requirements & Implementation**

### **1. Password Security **

#### **Implementation Details**

* **Hashing Algorithm**: `bcrypt` with 12 rounds of salting
* **Location**: `server/models/User.js`
* **Key Features**:

  * Automatic password hashing when a user is saved
  * Secure password comparison function
  * Password field hidden from queries (`select: false`)
  * Timing-safe comparisons to prevent side-channel attacks

#### **Password Validation**

```regex
^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[@$!%*?&])[A-Za-z\d@$!%*?&]{8,}$
```

* Minimum 8 characters
* Must include uppercase, lowercase, number, and special character

#### **Implementation Example**

```javascript
// Hash password before saving
userSchema.pre('save', async function(next) {
  if (!this.isModified('password')) next();
  const salt = await bcrypt.genSalt(12);
  this.password = await bcrypt.hash(this.password, salt);
});

// Compare passwords securely
userSchema.methods.comparePassword = async function(enteredPassword) {
  return await bcrypt.compare(enteredPassword, this.password);
};
```

#### **Extra Research**

* 12 salt rounds = good security/performance balance
* bcrypt adapts to hardware improvements over time
* Defends against brute force and rainbow table attacks

**Score Justification**: Exceeds standard — secure, modern, and well-tested.

---

### **2. DevSecOps Pipeline **

#### **Configuration**

**File**: `.circleci/config.yml`

The pipeline automates builds, testing, and security checks on each Git push.

#### **Pipeline Highlights**

**a) Static Application Security Testing (SAST)**

* Tool: **SonarCloud (SonarQube)**
* Checks code quality, vulnerabilities, and maintainability
* Runs automatically on every push

```yaml
sonarqube-scan:
  docker:
    - image: cimg/node:18.0
  steps:
    - checkout
    - node/install-packages
    - run:
        name: Run Tests with Coverage
        command: |
          cd server && npm test -- --coverage
          cd ../client && npm test -- --coverage --watchAll=false
    - sonarcloud/scan
```

**b) Software Composition Analysis (SCA)**

* Uses `npm audit` for dependency vulnerability checks
* Runs on both frontend and backend
* Only flags high-severity issues

```yaml
security-scan:
  steps:
    - run:
        name: Run npm audit
        command: |
          cd server && npm audit --audit-level=high
          cd ../client && npm audit --audit-level=high
```

**c) API Security Testing**
Includes:

* Rate limit checks
* Authentication and validation tests
* Security header verification

**d) Security Tool Tests**
Verifies rate limiting and brute force protection (`express-brute`).

#### **Pipeline Workflow**

```yaml
workflows:
  test-and-deploy:
    jobs:
      - test-backend
      - test-frontend
      - security-scan
      - sonarqube-scan
      - api-security-test
```

**Score Justification**: Exceeds rubric — includes automated scans, SAST, SCA, and API security testing.

---

### **3. Static Login **

#### **Implementation**

**File**: `server/routes/authRoutes.js`

```javascript
// Registration disabled – only preconfigured users
// router.post('/register', register);
router.post('/login', authLimiter, bruteLimiter, loginValidation, validate, login);
```

#### **Accounts**

* **Employee:** `employee1 / Employee123! / 9876543210`
* **Customer:** `testcustomer / Test123! / 1234567890`

#### **Security Measures**

* No new user registrations allowed
* Manual database creation only
* Role-based access enforced

**Why?**
To reduce unauthorized access and follow best practices common in enterprise-grade systems.

**Score Justification**: Meets and exceeds expectations — locked-down login with strong access control.

---

### **4. Overall Web App Functionality **

#### **Architecture**

* **Frontend:** React + Material-UI
* **Backend:** Node.js / Express
* **Database:** MongoDB (Mongoose)
* **Auth:** JWT tokens

#### **Security Setup**

* HTTPS enforced in production
* Security headers via **Helmet**
* Strict input validation (RegEx-based)
* Middleware for sanitization and XSS prevention
* Brute-force protection, rate limiting, and CSRF defense

#### **Attack Protections**

| Threat       | Defense                                  |
| ------------ | ---------------------------------------- |
| XSS          | Input sanitization, CSP                  |
| Injection    | Schema validation, parameterized queries |
| Brute Force  | express-brute, lockouts                  |
| CSRF         | Token-based auth                         |
| Clickjacking | Frame-ancestors, X-Frame-Options         |
| MITM         | HTTPS + HSTS                             |
| DoS          | Request limits, timeout handling         |

#### **Data Flow Overview**

1. **Customer Portal** – Request payments & view status
2. **Employee Portal** – Review and verify payments
3. **Security Layer** – Validates, sanitizes, and rate-limits all activity

**Score Justification**: Fully functional, secure, and clearly documented — exceeds rubric.

---

## **Security Summary**

| Category         | Highlights                                      |
| ---------------- | ----------------------------------------------- |
| Authentication   | JWT, bcrypt (12 rounds), RBAC, static login     |
| Input Security   | RegEx validation, sanitization, XSS prevention  |
| Rate Limiting    | Multi-tiered (API, auth, payments)              |
| Network Security | HTTPS, HSTS, Helmet, CORS                       |
| DevSecOps        | CircleCI, SonarQube, npm audit, automated tests |

---

## **Files Modified / Created**

**Modified**

* `authRoutes.js` – Disabled registration
* `security.js` – Added express-brute
* `.circleci/config.yml` – Enhanced pipeline
* `package.json` – Added dependencies

**New**

* `sonar-project.properties`
* `SECURITY_DOCUMENTATION.md`
* `security.test.js`
* `TASK3_COMPLETION_REPORT.md`

---

## **Setup Guide for Evaluators**

```bash
# Install dependencies
cd server && npm install
cd ../client && npm install

# Run app
cd server && npm start
cd ../client && npm start

# Run tests
cd server && npm test -- --testPathPattern=security
```

**Test Accounts**

* Employee → `employee1 / Employee123!`
* Customer → `testcustomer / Test123!`


## **Conclusion**

All **Task 3** objectives were met and, in many areas, exceeded.
The system now has strong password protection, a solid DevSecOps pipeline, static login for controlled access, and a fully functional secure web app.

Overall, the portal is **secure, stable, and production-ready** — with layered protection, continuous testing, and detailed documentation for evaluators.
